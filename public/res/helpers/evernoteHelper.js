define([ "jquery", "constants", "core", "utils", "storage", "logger",
		"eventMgr", "classes/AsyncTask" ], function($, C, core, utils, storage, log, eventMgr, AsyncTask) {
	
	var evernoteHelper = {};
	
	evernoteHelper.upload = function(title, content, callback){
		
	}

});